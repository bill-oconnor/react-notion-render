import { DropedProps } from '../../../hoc/withContentValidation';
export type Props = {
    className?: string;
    media?: DropedProps['media'];
};
declare function Video({ media, className }: Props): any;
export default Video;
